# Haskell Interview Practice

```bash
sudo apt install haskell-platform
# install the Haskell extension for VsCode
# cd into the directory that you want to create use as your project folder
cabal init
cabal configure
cabal build
# to run the executable: ./dist/build/PROJECTNAME/EXECUTABLE
# for example: ./dist/build/interview/interview
```